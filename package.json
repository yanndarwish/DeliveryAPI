{
	"name": "mariadb",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"database:up": "cat ./migrations/*.sql | mysql -h 127.0.0.1 -u root -p",
		"database:down": "mysql -h 127.0.0.1 -u root -p -e 'DROP DATABASE IF EXISTS dmt; DROP DATABASE IF EXISTS geostar;'"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
